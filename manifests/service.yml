apiVersion: v1
kind: Service
metadata:
    name: baliganorbiazurevotingappredis
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: baliganorbiazurevotingappredis